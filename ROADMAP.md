# BusBooking — тех. стек и план до деплоя

## Технологический стек

### Backend

| Компонент           | Выбор                                                  | Почему                                                                                                                                                                 |
| ------------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Фреймворк           | ASP.NET Core 8 Web API                                 | Уже есть готовая кодовая база (сущности, EF Core, контроллеры) — дешевле довести до ума, чем переписывать с нуля                                                       |
| ORM                 | Entity Framework Core 8                                | Уже используется, миграции частично написаны                                                                                                                           |
| База данных         | PostgreSQL                                             | Дешёвый/бесплатный managed-хостинг почти везде (Railway, Render, Fly.io, Supabase), в отличие от managed SQL Server                                                    |
| Аутентификация      | JWT (access + refresh токен), выдаётся API             | Фронтенд и бэкенд на разных доменах (Next.js на Vercel, API отдельно) — cookie-сессии между origin усложняют CORS/SameSite; JWT — стандарт для разделённой архитектуры |
| Хэширование паролей | `PasswordHasher<T>` (ASP.NET Core Identity) или BCrypt | Текущий голый SHA-256 без соли небезопасен                                                                                                                             |
| Валидация           | FluentValidation или DataAnnotations                   | Серверная валидация нужна независимо от клиентской — API публичный                                                                                                     |

### Frontend

| Компонент                 | Выбор                             | Почему                                                                                              |
| ------------------------- | --------------------------------- | --------------------------------------------------------------------------------------------------- |
| Фреймворк                 | Next.js (App Router) + TypeScript | SEO — реальная бизнес-задача, нужен SSR/SSG для публичных страниц                                   |
| Стилизация                | Tailwind CSS                      | Есть готовый визуальный дизайн (фиолетовый градиент, карточки) — быстро ложится на кастомный дизайн |
| Работа с API / кэш данных | TanStack Query                    | Поиск рейсов, доступность мест — данные нужно кэшировать и обновлять                                |
| Валидация форм            | Zod                               | TypeScript-нативная схема-валидация, переиспользуется на клиенте                                    |
| Карта маршрутов           | Leaflet + OpenStreetMap           | В дизайн-макете (`Map.jpg`) уже атрибуция Leaflet/OpenStreetMap — бесплатно, без API-ключей         |

### Инфраструктура и деплой

| Компонент                  | Выбор                                                | Почему                                                                                 |
| -------------------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Контейнеризация            | Docker (multi-stage: `dotnet/sdk` → `dotnet/aspnet`) | Портируемость backend+DB независимо от хостинга                                        |
| Хостинг backend + Postgres | Fly.io / Railway / Render                            | Простой деплой контейнера + managed Postgres в одном месте                             |
| Хостинг frontend           | Vercel                                               | Next.js — их фреймворк, деплой без конфигурации, SSR/ISR/Edge из коробки               |
| CI/CD                      | GitHub Actions                                       | build+test backend → Docker-образ → деплой; Vercel сам подхватывает пуши для фронтенда |

### Тестирование (сейчас отсутствует полностью)

| Компонент | Выбор                         | Почему                                                                                     |
| --------- | ----------------------------- | ------------------------------------------------------------------------------------------ |
| Backend   | xUnit + WebApplicationFactory | Стандарт для ASP.NET Core, тесты API-эндпоинтов без реального деплоя                       |
| Frontend  | Vitest + Playwright           | Vitest — юнит-тесты компонентов, Playwright — e2e критичного флоу (поиск → место → оплата) |

---

## План реализации до деплоя

### Фаза 0 — Консолидация

- [+] Выбрать `BusBookingDemo` как основной проект, `BusBooking` вывести из репозитория
- [+] Перенести улучшенную доменную модель из `BusBooking` (разделение `Route`/`Trip∏`, `Order`+`OrderSeat` вместо плоского `Booking`)
- [+] Исправить `SeatDetail` — занятость места должна считаться по `TripId`, а не по `BusId`

### Фаза 1 — Функциональные баги

- [ ] Логин: использовать `VerifyPassword(...)` вместо сравнения пароля в открытом виде
- [ ] `SeatDetailController`: убрать вложенный класс, привести имя контроллера/маршрута и папку `Views/...` в соответствие
- [ ] Добавить недостающие views (`Booking/Index`, `Users/Index`, `Trip/Login`) либо удалить action'ы, которые никуда не ведут
- [ ] `Logout`: добавить `await` перед `SignOutAsync`

### Фаза 2 — Безопасность

- [ ] Заменить SHA-256 на `PasswordHasher<T>`/BCrypt для паролей
- [ ] Убрать хардкод паролей в сид-данных и проверку админа по email — использовать поле `Role` из БД
- [ ] Вернуть `[Authorize(Roles = "admin")]` и `[ValidateAntiForgeryToken]` на все нужные actions
- [ ] Убрать connection string из `OnConfiguring` в DbContext — только через конфигурацию/секреты

### Фаза 3 — Переход на PostgreSQL

- [ ] Сменить EF Core провайдер с `Microsoft.EntityFrameworkCore.SqlServer` на `Npgsql.EntityFrameworkCore.PostgreSQL`
- [ ] Пересоздать миграции под Postgres
- [ ] Строка подключения — через переменные окружения, без `Trusted_Connection`

### Фаза 4 — Превращение MVC в Web API

- [ ] Перевести контроллеры на `ControllerBase` + `[ApiController]`, отдавать JSON вместо `View()`
- [ ] Заменить cookie-аутентификацию на JWT (access + refresh)
- [ ] Настроить CORS под домен фронтенда
- [ ] Подключить Swagger/OpenAPI для документации API

### Фаза 5 — Docker

- [ ] Multi-stage `Dockerfile` для API
- [ ] `docker-compose.yml` (API + Postgres) для локальной разработки
- [ ] `/health` эндпоинт для проверки хостингом

### Фаза 6 — Frontend (Next.js)

- [ ] Заскаффолдить Next.js + TypeScript + Tailwind
- [ ] Публичные страницы (Home, About Us, маршруты) — SSR/SSG
- [ ] Флоу бронирования (поиск, выбор мест, оформление) — клиентские компоненты
- [ ] Интегрировать TanStack Query для запросов к API
- [ ] Интегрировать карту маршрутов на Leaflet
- [ ] Реализовать логин/регистрацию против JWT API

### Фаза 7 — CI/CD и хостинг

- [ ] GitHub Actions: build + test backend, сборка Docker-образа
- [ ] Задеплоить backend + Postgres на Fly.io/Railway/Render
- [ ] Подключить Vercel-проект к репозиторию фронтенда
- [ ] Настроить переменные окружения/секреты на обеих платформах

### Фаза 8 — Тесты и полировка

- [ ] xUnit-тесты на логику авторизации и бронирования
- [ ] Playwright e2e-тест на критичный флоу бронирования
- [ ] Почистить закомментированный код и разноязычные комментарии
- [ ] Обновить README под фактическое состояние проекта

### Фаза 9 — Поддержка: апгрейд .NET (дедлайн — ноябрь 2026)

- [ ] .NET 8 — LTS, поддержка заканчивается в ноябре 2026 — апгрейд не опционален, а привязан к дате
- [ ] Апгрейдить сразу на **.NET 10** (текущая LTS, поддержка до ноября 2028), **не** на .NET 9 (STS, уже вне поддержки с мая 2026)
- [ ] Делать после стабилизации и деплоя MVP, под прикрытием тестов из Фазы 8
- [ ] Установить .NET 10 SDK, поднять `<TargetFramework>` в `.csproj` с `net8.0` на `net10.0`
- [ ] `dotnet build` — разобрать breaking changes
- [ ] Обновить версии NuGet-пакетов (EF Core, Identity и т.д.) под 10.x
- [ ] Прогнать полный набор тестов перед деплоем апгрейженной версии
      Точное число в выводе `dotnet build` гуляет (22/25/27/29 между прогонами) — это не
      нестабильность реальных проблем, а особенность MSBuild: одно и то же предупреждение
      может печататься несколько раз за один `build` (restore + design-time evaluation +
      сам build), финальный счётчик "N Warning(s)" — это его собственная (не всегда
      интуитивная) агрегация. Ни один из этих пакетов не указан в `.csproj` напрямую под
      такой версией — они приходят транзитивно через `Microsoft.AspNetCore.Identity.*` и
      `Microsoft.VisualStudio.Web.CodeGeneration.Design`. Чинить их — отдельная задача
      (`dotnet list package --vulnerable`, обновление/пересмотр зависящих от них пакетов),
      не относится к сегодняшней правке кода.
